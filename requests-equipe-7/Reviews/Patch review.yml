info:
  name: Patch review
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{url}}reviews/:id"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
