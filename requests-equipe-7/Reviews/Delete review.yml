info:
  name: Delete review
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{url}}reviews/:id"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
