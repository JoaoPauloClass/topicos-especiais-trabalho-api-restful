info:
  name: Delete categoria
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{url}}categorias/:id"
  params:
    - name: id
      value: "2"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
