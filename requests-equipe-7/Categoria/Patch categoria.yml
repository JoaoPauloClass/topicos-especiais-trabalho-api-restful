info:
  name: Patch categoria
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{url}}categorias/:id"
  params:
    - name: id
      value: "3"
      type: path
  body:
    type: json
    data: |-
      {
        "nome": "Terror 2"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
