info:
  name: Post Categoria
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/categorias"
  body:
    type: json
    data: |-
      {
        "nome": "RPG"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
