info:
  name: Alterar categoria
  type: http
  seq: 4

http:
  method: PUT
  url: "{{url}}categorias/:id"
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "nome": "terror"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
