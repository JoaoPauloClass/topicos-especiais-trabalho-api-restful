info:
  name: Post jogos
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}jogos"
  body:
    type: json
    data: |-
      {
        "titulo": "The witcher 2",
        "datapublicacao": "01-01-2008",
        "desenvolvedor": "CD Project Red",
        "distribuidora": "Não lembro",
        "categoria_id": "2"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
