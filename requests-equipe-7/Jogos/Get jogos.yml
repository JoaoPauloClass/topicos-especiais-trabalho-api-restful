info:
  name: Get jogos
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}jogos"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
